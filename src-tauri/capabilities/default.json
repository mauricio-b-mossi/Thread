{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for Thread windows",
  "windows": ["today", "floating"],
  "permissions": [
    "core:event:allow-listen",
    "core:window:allow-outer-position",
    "core:window:allow-start-dragging"
  ]
}
